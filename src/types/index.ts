export interface Service {
  title: string;
  description: string;
  icon: string;
}

export interface Testimonial {
  name: string;
  quote: string;
  role: string;
}

export interface ContactForm {
  name: string;
  email: string;
  message: string;
}